import React from 'react'

export const Selection = () => {
  return (
    <section className="selection-section">
      {/* Progress Bar / Step Indicator */}
      step 1
      {/* User selects their track */}
      selection
      {/* Multi-step form, methinks */}
      next
    </section>
  )
}

// For Each Page:

// Top: Header
// Randomize Dice up top
// List of Card Items
// onCardClick / onDiceClick, moves to next screen
// after completed, shows "Results Page"
//   In Results, can begin, or start over


// Idea: Create a "Learning Project"